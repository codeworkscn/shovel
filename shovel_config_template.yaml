P4:
  port : "${P4PORT}" # e.g. localhost:1666
  user : "${P4USER}"
  passwd : "${P4PASSWD}"
  client : "${P4CLIENT}"

P4integrate:
  source : ""  # P4integrate p4 depot source path
  target : ""  #  P4integrate p4 depot target path


ES:
  host : "localhost"  # ES host
  port : 9200         # ES port
  user : ~
  pswd : ~

EScompare:
  leftIndex: "index_left_*"
  rightIndex: "index_right_*"

